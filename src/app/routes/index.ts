import { Router } from 'express';

const router = Router();

const moduleRoutes = [
  // { path: 'Provide the path', element: 'Provide the element route' },
];

// moduleRoutes.forEach((route) => router.use(route.path, route.element));

export const AllRoutes = router;
